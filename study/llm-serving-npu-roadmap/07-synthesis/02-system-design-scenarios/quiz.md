## 객관식 1
긴 계약서를 많이 받는 B2B 요약 서비스에서 아침 피크 시간 `TTFT`가 급격히 악화된다. 가장 먼저 검토할 선택지로 가장 적절한 것은 무엇인가?

A. Windows ML을 바로 도입한다.  
B. `prefill`과 `decode` 병목을 분리해서 보고 disaggregated prefill 도입 가능성을 검토한다.  
C. 배포 대상이 서버이므로 무조건 QNN Execution Provider를 붙인다.  
D. 이미지 로고 두 개를 문서에 추가한다.

## 객관식 2
Windows 노트북 여러 기종에 회의 요약 앱을 배포해야 한다. Qualcomm, AMD, Intel 장치가 섞여 있고, 초기에 제품을 빨리 내야 한다. 가장 현실적인 출발점은 무엇인가?

A. 처음부터 세 벤더별 SDK 경로를 각각 직접 구현한다.  
B. 공통 추론 계층을 먼저 두고, 병목이 확인된 장치에 한해 벤더별 최적화 경로를 추가한다.  
C. 클라우드 전용으로 바꾸고 on-device 요구는 무시한다.  
D. 모델 크기만 줄이면 하드웨어 차이는 사라진다고 가정한다.

## 객관식 3
on-device NPU 실행을 켰는데 체감 속도가 오히려 나빠졌다. 첫 조사 항목으로 가장 우선해야 하는 것은 무엇인가?

A. 문서 글꼴과 색상을 바꾼다.  
B. fallback이 발생하는 연산과 실제 offload coverage를 확인한다.  
C. 즉시 speculative decoding을 추가한다.  
D. 사용자를 설득해 latency 목표를 없앤다.

## 짧은 서술형 1
`speculative decoding`과 `disaggregated prefill`이 각각 어떤 지연 구간을 겨냥하는지 3~4문장으로 설명하라. [S1] [S2]

## 짧은 서술형 2
Windows ML 같은 범용 경로를 먼저 선택하는 이유와, 그 이후에 QNN EP 또는 OpenVINO NPU 같은 특화 경로로 내려가야 하는 조건을 3~4문장으로 설명하라. [S3] [S4] [S6]

## 심화 설명형 1
다음 조건의 제품에 대해 스택 선택 논리를 설명하라.

- 제품: 사내 문서 검색과 요약을 제공하는 클라우드 서비스
- 제약: 긴 프롬프트가 많고, 오전 피크 시간 `TTFT` 목표가 엄격함
- 추가 조건: 운영팀 규모가 작아 복잡한 최적화는 신중해야 함

답변에는 아래 요소를 포함하라.

- 어떤 지연 항을 먼저 볼지
- 범용 serving과 특화 기능 중 무엇을 먼저 도입할지
- speculative decoding 도입 여부를 어떻게 판단할지

## 심화 설명형 2
다음 조건의 제품에 대해 시스템 설계를 제안하라.

- 제품: Windows 노트북에서 동작하는 오프라인 회의 요약 도우미
- 제약: Qualcomm, AMD, Intel 장치가 섞여 있음
- 목표: 개인정보를 장치 밖으로 내보내지 않고, 초기 배포 속도도 놓치지 않기

답변에는 아래 요소를 포함하라.

- 공통 경로와 벤더별 경로의 순서
- fallback이 사용자 경험에 미치는 영향
- 하이브리드 실행을 언제 긍정적으로 볼 수 있는지

## 정답 및 해설
### 객관식 정답
- 객관식 1 정답: B
- 객관식 2 정답: B
- 객관식 3 정답: B

### 객관식 해설
- 객관식 1: 긴 프롬프트와 `TTFT` 문제가 핵심이므로 `prefill` 병목을 분리해서 보는 접근이 우선이다. `Disaggregated Prefill V1`은 바로 이 상황을 겨냥하는 대표 기능이다. [S1]
- 객관식 2: 여러 장치를 빨리 지원해야 할 때는 공통 추론 계층이 초기 제품화에 유리하다. 이후 병목이 드러나는 장치에 대해서만 QNN EP, Ryzen AI hybrid, OpenVINO NPU 같은 특화 경로를 추가하는 편이 운영 리스크가 낮다. [S3] [S4] [S5] [S6]
- 객관식 3: NPU 사용 여부보다 실제 offload coverage와 fallback 위치가 더 중요하다. 지원되지 않는 연산 하나가 CPU나 다른 장치로 되돌아가면 체감 latency와 전력 효율이 함께 나빠질 수 있다. [S3] [S6]

### 짧은 서술형 예시 답안
- 짧은 서술형 1: `disaggregated prefill`은 긴 입력을 읽는 초반 단계의 병목을 분리해서 다루려는 전략이다. 반면 `speculative decoding`은 생성 단계에서 더 적은 체감 지연으로 토큰을 뽑기 위한 접근이다. 둘은 둘 다 "빠르게 만들기"라는 큰 목적은 같지만, 겨냥하는 지연 항이 다르다. 그래서 긴 프롬프트 서비스라고 해서 두 기능을 항상 함께 도입할 필요는 없다. [S1] [S2]
- 짧은 서술형 2: Windows ML 같은 범용 경로는 여러 Windows 장치에서 공통된 제품 골격을 빠르게 만드는 데 유리하다. 하지만 특정 장치에서 목표 latency나 전력 수치가 안 나오면 그때 QNN EP나 OpenVINO NPU 같은 특화 경로를 검토해야 한다. 즉, 범용 경로는 출발점이고, 특화 경로는 병목이 확인된 뒤의 선택지다. 장치군이 고정되어 있고 privacy 요구가 강할수록 특화 경로를 더 빨리 택할 수 있다. [S3] [S4] [S6]

### 심화 설명형 채점 포인트
- 심화 설명형 1: 좋은 답안은 `T_e2e = T_prefill + T_decode + T_transfer + T_fallback` 관점으로 병목을 나눈다. 먼저 범용 serving 위에서 계측을 잡고, 긴 프롬프트 때문에 `prefill`이 지배적이면 disaggregated prefill을 검토한다고 말해야 한다. speculative decoding은 생성 단계가 병목일 때만 후보로 올리고, draft model 운영비와 acceptance 조건을 함께 따져야 한다. 운영팀이 작다면 기능을 한꺼번에 켜기보다 병목이 큰 항부터 순차적으로 줄이는 접근이 적절하다. [S1] [S2]
- 심화 설명형 2: 좋은 답안은 Windows ML 같은 공통 경로를 먼저 두고, Qualcomm 장치에는 QNN EP, AMD 장치에는 hybrid workflow, Intel 장치에는 OpenVINO NPU를 후속 경로로 연결하는 순서를 제시한다. 또한 "NPU 실행"만 확인하지 말고 fallback이 어디서 발생하는지 계측해야 한다고 써야 한다. 하이브리드 실행은 순수 NPU만 고집할 때보다 응답성, 전력, 사용자 경험이 더 안정적일 수 있으므로 부정적으로만 볼 필요가 없다는 점을 설명하면 좋다. [S3] [S4] [S5] [S6]
