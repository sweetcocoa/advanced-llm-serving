# Prefill vs Decode 퀴즈

## 객관식 1
사내 정책 검색 챗봇이 있다. 평균 입력은 8,500토큰이고 평균 출력은 45토큰이다. 사용자는 "답이 나오기 시작할 때까지 오래 걸린다"고 말하지만, 첫 토큰 이후에는 금방 끝난다. 가장 먼저 점검할 항목으로 가장 적절한 것은 무엇인가?

1. `decode` step의 미세 커널 최적화
2. `TTFT`를 `queue`, `prefill`, `first decode`로 나눠 보는 것
3. 출력 길이를 늘려 더 많은 토큰을 생성하게 하는 것
4. 이미지 입력 기능을 추가하는 것

## 객관식 2
짧은 지시문으로 긴 코드 패치를 생성하는 서비스에서 첫 토큰은 빠르지만, 생성이 길어질수록 토큰 간 간격이 벌어진다. 가장 자연스러운 해석은 무엇인가?

1. `prefill`이 유일한 병목이므로 입력 길이만 줄이면 된다
2. `decode` step 누적 비용과 batching 품질을 먼저 의심해야 한다
3. prefix가 반복되지 않으니 항상 `prefill`이 느려진다
4. 첫 토큰이 빠르면 사용자 체감은 자동으로 해결된다

## 객관식 3
다음 중 분리형 serving을 우선 검토할 이유로 가장 적절한 것은 무엇인가?

1. 모든 요청 길이가 비슷해서 단일 풀이 매우 안정적일 때
2. 긴 문서 기반 요청과 긴 생성 기반 요청이 한 자원 풀에서 서로 SLA를 흔들 때
3. 출력이 거의 없어 `decode` 비용이 무시될 때
4. 공통 prefix가 전혀 반복되지 않을 때

## 짧은 서술형 1
다음 식이 왜 긴 입력 서비스에서 중요한지 3~4문장으로 설명하라.

$$
TTFT \approx T_{\mathrm{queue}} + T_{\mathrm{prefill}} + T_{\mathrm{first\ decode}}
$$

## 짧은 서술형 2
공통 시스템 프롬프트가 긴 서비스에서 KV cache 재사용이 왜 `prefill` 최적화 논의와 연결되는지 3~4문장으로 설명하라.

## 심화 설명형 1
다음 상황에서 운영자가 어떤 순서로 문제를 좁혀 가야 하는지 설명하라.

- 서비스: 사내 규정 검색 Q&A
- 평균 입력: 10,000토큰
- 평균 출력: 70토큰
- 관측: 피크 시간에 첫 토큰이 크게 늦어지지만, 생성이 시작된 뒤에는 금방 끝난다

`prefill`, `decode`, `queue`, KV cache 재사용 가능성을 모두 포함해서 6문장 이상으로 답하라.

## 심화 설명형 2
다음 상황에서 단일 풀 유지와 분리형 serving 중 무엇을 더 우선 검토할지 설명하라.

- 워크로드 A: 문서 요약, 입력 12,000토큰, 출력 100토큰
- 워크로드 B: 코드 생성, 입력 180토큰, 출력 950토큰
- 두 워크로드가 같은 시간대에 자주 몰린다

`TTFT`, 토큰당 생성 지연, 운영 복잡도, 자원 분리의 이점을 모두 포함해서 6문장 이상으로 답하라.

## 정답 및 해설
- 객관식 1 정답: 2. 입력이 길고 출력이 짧은 서비스에서는 첫 토큰 이전 구간이 사용자 체감의 대부분을 만든다. 그래서 평균 latency 하나보다 `TTFT`를 `queue`, `prefill`, `first decode`로 나눠 보는 것이 먼저다.
- 객관식 2 정답: 2. 긴 출력 서비스는 `decode` step이 길게 누적되므로, 첫 토큰이 빠르더라도 active sequence 증가나 batching 품질 저하가 체감 성능을 악화시킬 수 있다.
- 객관식 3 정답: 2. 긴 입력 workload와 긴 출력 workload가 같은 풀에서 경쟁하면 한쪽은 `TTFT`, 다른 쪽은 생성 흐름이 나빠질 수 있다. 이런 상황이 분리형 serving을 검토할 가장 설득력 있는 이유다 [S4].
- 짧은 서술형 1 예시: 이 식은 첫 토큰 지연을 한 숫자로 보지 말고 세 구간으로 분해하라는 뜻이다. 긴 입력 서비스라면 `T_prefill`이 먼저 커질 가능성이 높고, 혼잡 시간대에는 `T_queue`도 함께 커질 수 있다. 따라서 운영자는 평균 응답 시간보다 `TTFT`의 구성 항목을 먼저 봐야 한다. 이 식은 "느리다"를 `prefill` 병목과 스케줄링 병목으로 나눠 생각하게 해 준다.
- 짧은 서술형 2 예시: 공통 시스템 프롬프트가 길면 매 요청마다 같은 앞부분을 반복해서 읽게 된다. 이 앞부분의 KV cache를 재사용할 수 있으면 반복 `prefill` 비용을 줄여 첫 토큰 지연 완화에 도움이 된다 [S3]. 다만 효과는 실제로 같은 prefix가 얼마나 자주 등장하는지에 달려 있다. 재사용 가능한 prefix가 적으면 기능을 켜도 체감 차이는 작다.
- 심화 설명형 1 해설 포인트: 이 상황은 전형적인 긴 입력·짧은 출력 패턴이므로 `decode`보다 `prefill`과 `queue`를 먼저 보는 것이 맞다. 첫 토큰 지연이 피크 시간에만 급증한다면 `T_queue`와 긴 입력 처리량이 겹친 상황일 수 있다. 먼저 `TTFT`를 분해해 queue와 prefill 중 무엇이 더 크게 튀는지 확인해야 한다. 첫 토큰 이후 생성이 짧고 안정적이라면 decode 튜닝은 우선순위가 낮다. 또한 공통 시스템 프롬프트나 반복 문서 머리말이 있다면 KV cache 재사용이 `prefill` 부담을 줄일 후보가 된다 [S3]. 핵심은 "모델이 느리다"가 아니라 "첫 토큰 이전 구간이 왜 길어졌는가"로 질문을 바꾸는 것이다.
- 심화 설명형 2 해설 포인트: 워크로드 A는 긴 `prefill` 부담이 크고, 워크로드 B는 긴 `decode` 반복 부담이 크다. 둘이 같은 시간대에 몰리면 단일 풀에서 `TTFT`와 토큰당 생성 지연이 서로 다른 방식으로 흔들릴 수 있다. 이런 경우 분리형 serving은 두 workload를 다른 자원 정책으로 다뤄 충돌을 줄이는 선택지가 된다 [S4]. 다만 단일 풀이 더 단순하므로, 실제로 SLA 충돌이 있는지 지표로 먼저 확인해야 한다. 만약 A의 첫 토큰 SLA와 B의 스트리밍 안정성을 동시에 만족시키기 어렵다면 분리형 serving 검토 우선순위가 올라간다. 반대로 충돌이 작고 운영 여유가 부족하다면 단일 풀에 캐시 재사용이나 decode 중심 튜닝을 먼저 적용하는 접근이 더 현실적일 수 있다.
